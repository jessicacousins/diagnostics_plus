# Nexus Diagnostics (Vite + React)

Browser diagnostics dashboard. Runs fully client-side.

- Metrics depend on browser support; the UI falls back when APIs are unavailable.
- The **Export Snapshot (PDF)** button captures the dashboard and downloads a single-page PDF.

## Website

```
https://diagnosticsai.netlify.app/
```
